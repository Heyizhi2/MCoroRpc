<!--
 * @Author: 来自火星的码农 15122322+heyzhi@user.noreply.gitee.com
 * @Date: 2026-05-17 12:41:50
 * @LastEditors: 来自火星的码农 15122322+heyzhi@user.noreply.gitee.com
 * @LastEditTime: 2026-05-18 12:13:44
 * @FilePath: /MCoroRpc/答辩前准备.md
 * @Description: 这是默认设置,请设置`customMade`, 打开koroFileHeader查看配置 进行设置: https://github.com/OBKoro1/koro1FileHeader/wiki/%E9%85%8D%E7%BD%**AE*
-->
### 各个组件细节


#### 协程
    - promise_type的封装
    - 父子协程的传递--->可通过co_await执行子协程的逻辑，子协程在执行完毕之后，将父协程的唤醒回调注册到eventloop中的就绪队列中，由调度器执行唤醒逻辑。
    - 协程的schedule---->向eventloop注册-->call_soon()--->将协程id和唤醒回调注册到eventloop当中，当协程调度器启动时会立即执行唤醒逻辑。
#### **调度器**
    
#### **channel**
    channel作为同步原语，即可往里面写数据，也可从中读数据，当缓冲区仍然有空间时，通过send方法，将数据存写缓冲区当中，当缓冲区未满，则将数据直接放入buffer中，当缓冲区满时，通过
  - readawaiter/writeawaiter,
    执行逻辑，记录本地的handle,并执行pushWriter。
    channel的awaiter定制不经过eventloop的唤醒。

   - buffer队列
     用于存放需要派发的数据
   
   - pushReader
      1. 情况1，当读队列中的有正在等待的写等待器，队首出队，唤醒等待的写等待器，传递数据。
      2. 情况2, 当缓存区不为空，等待队列为空，从缓存区中取数据，唤醒当前的read。
      3. 无数据，无匹配的writer，进入队列中，当前阻塞。
    
#### **tcpstream**
#### **rpcprovider**
#### **rpcchannel**
#### **zkclient**
#### **rpc_server_impl**





答辩稿子
